[project]
name = "jupyterlite-hivdb-drug-resistance-maps"
version = "0.1.0"
description = "Teaching examples for geospatial mapping of Stanford HIVDB surveillance data with JupyterLite"
requires-python = ">=3.11"
license = {text = "MIT"}

[tool.pytest.ini_options]
testpaths = ["tests"]
